export interface ITrip {
  id: number;
  city: string;
  imgURL: string;

  startDate: string;
  endDate: string;
}

export interface ITripInitial {
  city: string;
  startDate: string;
  endDate: string;
}
